// pages/share/share.js

Page({
  /*
  data: {
    imgUrl: 暂无
    
  },
  saveCode() {
    const { imgUrl } = this.data
    wx.downloadFile({
      url: imgUrl,
      success: (res) => {
        wx.saveImageToPhotosAlbum({
          filePath: res.tempFilePath
        })
      }
    })
  }
  */
})
